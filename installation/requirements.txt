# ============================================================================
# Agent Attack Framework — Python dependencies
# ============================================================================
# Tested with: Python 3.11, PyTorch 2.9.0+cu128, JAX 0.5.3, CUDA 12.8
#
# One-step install:  bash installation/install.sh [ENV_NAME]   (default env: vast)
# Manual install:    see installation/INSTALL.md
# ============================================================================

# ---------------------------------------------------------------------------
# 1. Core RL / training framework
# ---------------------------------------------------------------------------
openpipe-art[backend,langgraph]==0.5.9
vllm==0.11.2
trl==0.24.0
unsloth==2026.2.1
transformers==4.57.2

# ---------------------------------------------------------------------------
# 2. Agent orchestration (LangGraph / LangChain)
# ---------------------------------------------------------------------------
langgraph==1.0.8
langchain-core==1.2.12

# ---------------------------------------------------------------------------
# 3. LLM clients & observability
# ---------------------------------------------------------------------------
openai>=2.21.0
litellm>=1.81.11
weave==0.52.28

# ---------------------------------------------------------------------------
# 4. JAX ecosystem (Pi0.5 VLA via OpenPI)
# ---------------------------------------------------------------------------
jaxlib==0.5.3
flax==0.10.2
equinox==0.13.4
orbax-checkpoint==0.11.13
chex==0.1.86
jaxtyping==0.2.36
augmax==0.4.1

# ---------------------------------------------------------------------------
# 5. Robotics / simulation (MuJoCo, robosuite, LIBERO)
# ---------------------------------------------------------------------------
mujoco==3.5.0
robosuite==1.4.0
bddl==3.6.0
easydict==1.13
h5py==3.15.1
PyOpenGL==3.1.10
PyOpenGL-accelerate==3.1.10
termcolor==3.3.0
matplotlib
gym>=0.26.0

# ---------------------------------------------------------------------------
# 6. ML / scientific computing
# ---------------------------------------------------------------------------
numpy>=2
scipy==1.17.0
scikit-learn==1.8.0
scikit-image==0.26.0
opencv-python-headless
Pillow==12.1.0
imageio==2.37.2

# torchcodec: unsloth imports it at init time; 0.5 crashes on PyTorch 2.9+
torchcodec>=0.6.0

# ---------------------------------------------------------------------------
# 7. Tokenizers / model utilities
# ---------------------------------------------------------------------------
sentencepiece==0.2.1
einops==0.8.2
dm-tree==0.1.9
beartype==0.19.0
tyro==1.0.6
ml-collections==1.0.0
numpydantic==1.7.0
treescope==0.1.10
tqdm-loggable==0.3
flatbuffers==25.12.19

# ---------------------------------------------------------------------------
# 8. General utilities
# ---------------------------------------------------------------------------
gcsfs
pydantic==2.12.5
python-dotenv==1.2.1
httpx==0.28.1
tqdm==4.67.3
packaging==25.0
nest-asyncio==1.6.0
hydra-core==1.3.2
omegaconf==2.3.0
